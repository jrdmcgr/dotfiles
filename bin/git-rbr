#!/bin/bash
set -euo pipefail

BRANCH="$1"
git checkout develop
git pull
git checkout $BRANCH
git rebase develop

# Check if all modified files are compiled files
# else break

# npm run prod
# git add .
# git rebase --continue

# check to see if we are still rebasing another patch
# if so, then repeat 10-15
# else continue

# git push -f
